//call future method
DepartmentTriggerHandler.createDefaultEmployee(new set<id>());

AccountQueueableExample q = new AccountQueueableExample();
q.accList = new list<account>();
//how to enqueue queueable
sytem.enqueue(q);