integer i = 5;
integer j = 7;
//integer z = i+j;//if integer is null, we cannot do any operation on it.
system.debug(z);

integer z = 5/6;
system.debug(z);


string s;
string s1 = 'this';
string s2 = s+s1;//if string is null, we can concate. We cant call any string methods.
string s3 = s1 + j;//will this work? this7
system.debug(s2);

 
